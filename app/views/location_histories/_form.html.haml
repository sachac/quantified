= form_for @location_history, class: 'form-horizontal' do |f|
  -if @location_history.errors.any?
    #error_explanation
      %h2= "#{pluralize(@location_history.errors.count, "error")} prohibited this location_history from being saved:"
      %ul
        - @location_history.errors.full_messages.each do |msg|
          %li= msg

  .control-group
    .controls= f.submit 'Save', class: 'btn btn-primary'
