= setup_page 'categories', @record.record_category.name, 'time/nav'
= record_category_breadcrumbs @record.record_category

= render 'form'

= link_to 'Show', @record
\|
= link_to 'Back', records_path
