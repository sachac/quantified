= semantic_form_for @goal do |f|
  = f.inputs do
    = f.input :label
    = f.input :expression
    = f.input :period, :as => :select, :collection => ["weekly", "monthly", "daily"]
  .actions
    = f.submit 'Save'
