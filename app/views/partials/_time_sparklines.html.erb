<div style="font-size: 20px">
<table>
<% cat.sort.each do |label|
   list = time_graphs[label] %> 
<tr><td><%= label %></td><td><span class="sparkline-bar"><%= list.join(',') %></span><td><%= '%.1f' % list.sum %></td></tr>
<%   end %>
</table>

</div>
   
